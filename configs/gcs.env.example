# Copy to gcs.env and source before make replay / make ui (do not commit gcs.env).
export GCS_RESULTS_BUCKET=gpu-tpu-benchmark-storage
export GCS_RESULTS_PROJECT=gpu-tpu-benchmark-results
export GCS_RESULTS_PREFIX=latest
export GCS_UPLOAD=1
export RESULTS_SOURCE=gcs

# Optional: fail replay if upload fails
# export GCS_UPLOAD_REQUIRED=1
